@PointClass base(BaseEntityPoint) 
	appliesto(Mesa) 
	iconsprite("editor/info_target.vmt") = material_timer
	[
    length(float) : "Length" : 30 : "Length"
	input start(float) : "Start"
	input stop(float) : "Stop"

	output OnStart(void) : ""
	output OnStop(void) : ""
	output OnFinish(void) : ""
	]
