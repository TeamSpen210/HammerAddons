@NpcClass base(BaseNPC) 
	appliesto(HL2_ENTITIES)
	studio("models/Weapons/w_grenade.mdl") 
= npc_grenade_frag: "Hand Grenade"
	[

	// Inputs
	input SetTimer(float) : "This input makes the grenade live, and specifies how many seconds before it explodes."
	]
