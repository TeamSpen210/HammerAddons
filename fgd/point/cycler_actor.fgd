
@PointClass base(BaseNPC, RenderFields, Shadow) 
	appliesto(ASW, EP1, EP2, HL2, P1, P2) 
	studio() = cycler_actor: "Actor Cycler"
	[
	model(studio) : "Model"
	sentence(string) : "Sentence Group"

	// Inputs
	input Alpha(integer) : "Set Alpha Value"
	]
