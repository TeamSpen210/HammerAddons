@PointClass base(BaseEntityPoint) 
	appliesto(EP1, EP2, HL2, P1, ASW, Mesa)
	line(255 255 255, targetname, vortigaunt)
= script_tauremoval: "Script: Custom entity used to handle the tau removal sequence in coast. (unused)"
	[
	vortigaunt(target_destination) : "Vortigaunt to use"

	// Inputs
	input StartScript(void) : "Start the script."
	input RemoveTau(void) : "Start removing the Tau cannon now."
	]
