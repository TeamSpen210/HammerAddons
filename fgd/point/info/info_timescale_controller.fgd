@PointClass base(BaseEntityPoint) 
	appliesto(Mesa) 
	iconsprite("editor/info_target.vmt") = info_timescale_controller : "host_timescale controller entity."
	[
	input SetTimescale(float) : "Sets the value of host_timescale."
	input RestoreTimescale(void) : "Restores the value of host_timescale back to what it was previously."
	]
