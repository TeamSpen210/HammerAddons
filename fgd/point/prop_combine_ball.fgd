
@PointClass base(BasePropPhysics, BasePropPhysics) 
	appliesto(ASW, EP1, EP2, HL2, P2) 
	studioprop() 
	studio("models/effects/combineball.mdl") = prop_combine_ball: "A prop that physically simulates as a single rigid body. It can be constrained to other physics objects using hinges or other constraints. It can also be configured to break when it takes enough damage.|||An earlier Half-Life version of the Energy Pellet."
	[

	// Inputs
	input Explode(void) : "Explode"
	]
