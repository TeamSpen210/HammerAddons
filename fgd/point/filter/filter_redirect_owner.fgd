@FilterClass base(BaseRedirectFilter) 
	autovis(Logic, Filters)
	appliesto(MBase)
	iconsprite("editor/filter_redirect.vmt")
= filter_redirect_owner :
	"A filter that passes the activator's owner entity to a separate filter."
	[
	@resources []
	]
