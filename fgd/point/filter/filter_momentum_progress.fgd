@FilterClass base(filter_base)
	appliesto(MOMENTUM)
= filter_momentum_progress : "Filter that checks to see if the player is at or above the given progress number."
	[
	progress_check(integer) : "Minimum req. prog. num" : -1 : "The minimum progress number that the player needs to be at in order for the filter to accept them."
	]
