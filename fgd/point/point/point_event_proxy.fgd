@PointClass base(BaseEntityPoint) 
	appliesto(ASW)
= point_event_proxy: "An entity that allows fires simple game events from map logic."
	[
	eventname(string) : "Event Name" : : "Name of the event fired"
	activatorasuserid(boolean) : "Activator as 'userid' field" : 1 : "Use the input activator as the userid field in the event fired." 
	
	// Inputs
	input GenerateEvent(void) : "Generate the game event."
	]
