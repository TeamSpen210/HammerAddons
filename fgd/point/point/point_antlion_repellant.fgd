@PointClass base(BaseEntityPoint) 
	appliesto(EP1, EP2, HL2, P1) 
	sphere(repelradius) 
	iconsprite("editor/point_antlion_repellant.vmt")
	color(0 0 255) = point_antlion_repellant: "Antlion Repellant"
	[
	repelradius(float) : "Repell radius" : 512 : "Antlions aren't allowed to be inside this radius"

	// Inputs
	input Enable(void) : "Enable"
	input Disable(void) : "Disable"
	]
