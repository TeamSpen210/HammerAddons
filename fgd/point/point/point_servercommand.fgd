@PointClass base(BaseEntityPoint) 
	color(200 0 0) 
	iconsprite("editor/point_servercommand.vmt") 
= point_servercommand: "An entity that issues commands to the server console."
	[

	// Inputs
	input Command[-MOMENTUM](string) : "Command to execute."
	input Command[+MOMENTUM](string) : "Command(s) to execute, separated by semicolons."
	]
