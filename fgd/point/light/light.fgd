@PointClass base(Targetname, BaseLight)
	light() 
	iconsprite("editor/light.vmt") 
	sphere(_distance) 
	line(255 255 255, targetname, target)
= light: "An invisible omnidirectional lightsource."
	[
	spawnflags(flags)  =
		[
		1: "Initially dark" : 0
		]
	]
