
@PointClass base(Targetname, Targetname) 
	appliesto(ASW, EP1, EP2, HL2, P2, TF2, CSGO) 
	size(-8 -8 -8, 8 8 8) 
	color(200 0 0) 
	iconsprite("editor/point_clientcommand.vmt") = point_clientcommand: "An entity that issues commands to the client console, as if it was typed in by the player (if activator is a player, or the local player in single player)."
	[

	// Inputs
	input Command(string) : "Command to execute."
	]
