@PointClass base(BaseEntityPoint) 
	appliesto(HL2_ENTITIES) 
	autovis(AI, AI Citizen Response System)
= ai_citizen_response_system: "If placed in the level, will manage citizens responses to player's actions."
	[

	// Inputs
	input ResponseVitalNPC(void) : "Fire the VitalNPC Died response."
	]
