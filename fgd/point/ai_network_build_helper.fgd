@PointClass base(BaseEntityPoint) 
	appliesto(+engine, ASW, EP1, EP2, HL2, P1) 
= ai_network_build_helper: "Spawned in during the nodegraph loading process to perform mod-specific adjustments."
	[
	@resources []
	]
