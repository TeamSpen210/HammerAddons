@SolidClass base(HopTrigger)
	appliesto(MOMENTUM)
= trigger_momentum_multihop : "Trigger that allows for multiple hops inside of it, and teleports the player if they stay inside for longer than the hold time."
	[
	]
