@SolidClass base(trigger_capture_area) 
	appliesto(TF2) = trigger_timer_door: "A trigger that will open/close the door base filtered objects."
	[
	door_name(string) report: "Door Name" : : "Name of the door entity the trigger is attaching to."

	@resources []
	]
