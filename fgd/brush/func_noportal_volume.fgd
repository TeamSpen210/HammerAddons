
@SolidClass base(Targetname, Parentname, Origin) 
	appliesto(P1, P2) = func_noportal_volume: "A region in which no portal can be placed|||A region which prevents portal placement."
	[
	spawnflags(flags) : "spawnflags" =
		[
		1: "Start inactive" : 0
		]


	// Inputs
	input Deactivate(void) : "Turn off blocking functionality"
	input Activate(void) : "Turn on blocking functionality"
	input Toggle(void) : "Toggle blocking functionality"
	]
