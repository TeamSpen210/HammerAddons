
@SolidClass base(TriggerOnce) 
	appliesto(ASW, EP1, EP2, HL2, P1, P2, TF2, CSGO) 
	line(255 255 255, targetname, filtername) = trigger_once: "A trigger volume that removes itself after it is triggered once."
	[

	// Outputs
	output OnTrigger(void) : "Fired whenever the trigger is activated."
	]
